/*import express from 'express';
import jasperController from '../controllers/jasper.controller.js';

const router = express.Router();

router.get('/api/jasper/reports', jasperController.getReports);
router.get('/api/jasper/report/:reportUri(*)', jasperController.getReport);

export default router;*/