// API configuration
import { BASE_URL } from '../../../config.js';
export const API_CONFIG = {
    BASE_URL: `${BASE_URL}`,
    ENDPOINTS: {
        LEGAL_ENTITY: '/registrar/cliente/juridico',
        LOCATIONS: '/lugares'
    }
};